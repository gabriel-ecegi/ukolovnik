import { Box } from "@mui/material";
import { AuthenticatedLayout } from "Shared/Layouts/AuthenticatedLayout";

export const TasksPage: React.FunctionComponent = (_) => {
  return (
    <AuthenticatedLayout title="Úkolovník">
      <Box />
    </AuthenticatedLayout>
  );
};
